{% extends 'layouts/two-column.html.twig' %}

{% block title %}Company index{% endblock %}

{% block content %}
    <h1>Companies</h1>
    <h2> Count: {{ companies|length }} </h2>

    <table class="table">
        <thead>
            <tr>
                <th>№</th>
                <th>Title</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for company in companies %}
            <tr>
                <td>{{ loop.index }}</td>
                <td>{{ company.title|e|upper }}</td>
                <td>
                    <a href="{{ path('company_show', {'id': company.id}) }}">show</a>
                    <a href="{{ path('company_edit', {'id': company.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('company_new') }}">Create new</a>
{% endblock %}
