{% extends 'layouts/two-column.html.twig' %}

{% block title %}Edit Skill{% endblock %}

{% block content %}
    <h1>Edit Skill</h1>

    {{ include('skill/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('skill_index') }}">back to list</a>

    <a href="{{ path('skill_delete', {'id': skill.id}) }}">delete</a>
{% endblock %}
